/**
 * Public surface of the browser-control feature. Import ONLY from here.
 * Everything else under this folder is internal and may be refactored freely
 * (including swapping the driver implementation).
 */
export { browserControlModule, BROWSER_CONTROL_MODULE_ID } from './module';
